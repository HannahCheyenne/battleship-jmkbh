import React, { Component } from 'react'
import './demogame'
//this component will join the demo version of the game against the ai
class DemoGame extends Component{
    render(){
        return(
            <button>Demo Game</button>
        )
    }
}

export default DemoGame